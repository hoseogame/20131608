#pragma once
#include <iostream>
#include <conio.h>
#include <cstring>
#include <Windows.h>
#include <ctime>
#include "Screen.h"
typedef enum _GAME_STATE { INIT, READY, RUNNING, SUCCESS, FAILED, STOP, RESULT } GAME_STATE;
